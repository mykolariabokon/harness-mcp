node_modules/
build/
*.log

# Local tooling state
.ai/
.claude/settings.local.json
# Machine-specific: holds an absolute path to this checkout. The README shows the
# snippet; each machine writes its own.
.mcp.json

# A harness inside this repo: the markdown spec is committable, the rest is not.
harness/harness.json
harness/.harness.json.*.tmp
harness/.cache/
harness/config.json
__pycache__/
