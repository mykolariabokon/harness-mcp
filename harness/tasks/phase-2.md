<!-- Generated by Harness MCP. This file is the harness — the source of truth
     the agent implements from. Do not hand-edit: propose a change through
     harness_propose_change so a human can review the diff. -->

# Phase 2

## STEP-03 — CI на GitHub Actions

Репозиторій публічний, але збірка й тестиганяються лише локально. Потрібен workflow на push і pull_request.

verify: `.github/workflows — зелений прогін npm ci && npx tsc --noEmit && npm test`

## STEP-04 — Розширити harness_verify до семантики

Зараз перевірка структурна: оголошені шляхи, неврахований верхній рівень, regex-правила, кроки без verify. Сенсу коду вона не читає — тут може допомогти ProjectMind MCP через параметр analysis.

verify: `npm test — розбіжність «модуль існує, але робить не те, що каже харнес» знаходиться`
