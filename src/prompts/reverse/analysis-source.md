The inventory came from a semantic analysis of the codebase, not a shallow file
walk: symbols and dependencies in it are resolved, so trust its module boundaries
over your own reading of directory names.
