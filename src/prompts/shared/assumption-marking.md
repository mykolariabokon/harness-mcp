- Anything the evidence does not settle is confidence "assumption" and MUST carry
  the `question` you would ask the human. An assumption without a question is
  rejected: it is a guess nobody can correct.
